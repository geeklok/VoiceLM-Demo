# 部署脚本 deploy/sync.sh 的本地配置 (复制为 deploy/sync.env 后填写)。
# deploy/sync.env 已被 .gitignore 忽略, 不会进版本库, 用来存放线上信息。
#
# 必填:
DEPLOY_HOST=1.2.3.4
DEPLOY_KEY=~/.ssh/your_key.pem
# 可选 (默认 root / /opt/asr):
# DEPLOY_USER=root
# DEPLOY_PATH=/opt/asr
